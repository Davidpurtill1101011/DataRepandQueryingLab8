import React from 'react';

export class Footer extends React.Component{
    render(){
        return(
            // specifying what we want to dislay in the footer component
            <div>
                <h1>This is the Footer Component</h1>
            </div>
        );
    }
}